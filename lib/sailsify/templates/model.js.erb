module.exports = {
  tableName: '<%= @model.table_name %>',
  attributes: {
    <% @model.columns.each do |c| %>
        <%= c.name.camelize(:lower) %>: {
          type: '<%= c.type %>',
          columnName: '<%= c.name %>'
        },
    <% end %>
    <%= raw @model.render_associations %>
  }
};
