module.exports = {
  tableName: '<%= @model.table_name %>',
  attributes: {
    <% @model.columns do |c| %>
        <%= c.name.camelize(:lower) %>: {
          type: <%= c.type %>,
          columnName: <% c.name %>
        }
    <% end %>
  }
}
